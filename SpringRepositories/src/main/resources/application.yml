spring:
  datasource:
    url: jdbc:mysql://localhost:3306/devdb
    username: root
    password: admin
  jpa:
    database: mysql
    show-sql: true
    hibernate:
      ddl-auto: create
  jackson:
    serialization:
      fail-on-empty-beans: false